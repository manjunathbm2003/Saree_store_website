services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: saree_store
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
